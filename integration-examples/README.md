# Приклади інтеграції

Ця папка містить готові приклади інтеграції з CRM системою для різних мов програмування.

## Доступні приклади

### 📝 JavaScript/Node.js
**Файл:** `example.js`

Використовуйте для:
- Node.js серверів
- Веб-застосунків
- Serverless функцій

```bash
node example.js
```

### 🐍 Python
**Файл:** `example.py`

Використовуйте для:
- Django/Flask застосунків
- Скриптів автоматизації
- Data processing pipelines

```bash
python example.py
```

### 🐘 PHP
**Файл:** `example.php`

Використовуйте для:
- WordPress сайтів
- Laravel застосунків
- Класичних PHP проектів

```bash
php example.php
```

## Як використовувати

### 1. Виберіть приклад для вашої мови

Скопіюйте відповідний файл у ваш проект.

### 2. Налаштуйте конфігурацію

Змініть ці значення у файлі:

```javascript
const API_URL = 'https://your-domain.com/api/integration'; // Ваш домен
const API_KEY = 'your_api_key_here'; // Ваш API ключ
```

```python
API_URL = 'https://your-domain.com/api/integration'  # Ваш домен
API_KEY = 'your_api_key_here'  # Ваш API ключ
```

```php
define('API_URL', 'https://your-domain.com/api/integration'); // Ваш домен
define('API_KEY', 'your_api_key_here'); // Ваш API ключ
```

### 3. Замініть utm_source

Обов'язково вкажіть вашу унікальну мітку:

```javascript
utm_source: 'your_partner_name'  // Замініть на вашу унікальну мітку
```

Це дозволить ідентифікувати ліди з вашої системи.

### 4. Викличте функцію

Кожен приклад містить готову функцію для відправки ліда:

**JavaScript:**
```javascript
sendLeadToCRM(leadData)
```

**Python:**
```python
send_lead_to_crm(lead_data)
```

**PHP:**
```php
sendLeadToCRM($leadData)
```

## Формат даних ліда

Мінімальні обов'язкові поля:

```json
{
  "name": "Ім'я клієнта",
  "phone": "+380501234567"
}
```

Рекомендовані поля:

```json
{
  "name": "Ім'я клієнта",
  "phone": "+380501234567",
  "email": "client@example.com",
  "location": "Київ, Україна"
}
```

Додаткові UTM мітки (опціонально):

```json
{
  "name": "Ім'я клієнта",
  "phone": "+380501234567",
  "email": "client@example.com",
  "location": "Київ, Україна",
  "utm_medium": "cpc",
  "utm_campaign": "summer_sale",
  "utm_content": "banner_top",
  "utm_term": "keyword"
}
```

## Відповідь API

### Успішна відповідь:

```json
{
  "success": true,
  "message": "Lead created successfully",
  "status": "UC_HSS56X",
  "leadId": "507f1f77bcf86cd799439011",
  "isDuplicate": false
}
```

### Дублікат:

```json
{
  "success": true,
  "message": "Lead created successfully",
  "status": "DUPLICATE",
  "leadId": "507f1f77bcf86cd799439012",
  "isDuplicate": true
}
```

### Помилка:

```json
{
  "success": false,
  "message": "Phone number is required"
}
```

## Тестування

Перед використанням в продакшені, протестуйте інтеграцію:

1. Відправте тестовий лід
2. Перевірте відповідь API
3. Перевірте чи лід з'явився в CRM
4. Відправте той самий лід повторно для перевірки дублікатів

## Підтримка

При виникненні проблем:

1. Перевірте API ключ
2. Перевірте формат даних
3. Перевірте логи сервера
4. Зверніться до адміністратора CRM

## Повна документація

Для детальної інформації дивіться:
- [INTEGRATION_API.md](../INTEGRATION_API.md) - Повна документація API
- [INTEGRATION_QUICK_START.md](../INTEGRATION_QUICK_START.md) - Швидкий старт
